#include<iostream>//ac
#include<cstdio>
#include<string>
using namespace std;
int main() {
    int n;
    cin>>n;
    if(n>=90) {
        cout<<"gong xi ni kao le "<<n<<" fen!";
    }
    else {
        cout<<"kao le "<<n<<" fen bie xie qi!";
    }
    return 0;
}
